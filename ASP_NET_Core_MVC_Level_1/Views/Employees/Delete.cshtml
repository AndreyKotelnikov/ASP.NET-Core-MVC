@model ASP_NET_Core_MVC.ViewModels.EmployeeView

@{
    ViewData["Title"] = "Delete employee with id: " + Model.Id;
}

<h2>@ViewBag.Title</h2>

<h3 class="text-danger">Are you sure want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.SecondName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.SecondName)
        </dd>
    </dl>
    
    <form asp-action="DeleteConfirmed" method="post" asp-route-id="@Model.Id">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
